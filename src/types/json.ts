export type HomePageJson = {
  title: string;
  description: string;
  link?: string; // Optional link property.
};

export type AboutPageJson = {
  value: string;
  title: string;
  content: string;
};
