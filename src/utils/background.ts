/**
 * Returns the background style classes for light and dark modes.
 * @returns {string} The background style classes.
 */
export function getLightBackgroundStyle(): string {
  return "bg-[#fcfcfc] dark:bg-gray-900";
}
